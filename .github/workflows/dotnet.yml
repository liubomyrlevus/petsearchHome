name: .NET Build & Test (PostgreSQL)

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: 12345
          POSTGRES_DB: petsearchhome
        ports:
          - 5432:5432
        options: >-
          --health-cmd="pg_isready -U postgres"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=5

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x

      - name: Verify .NET version
        run: dotnet --info

      - name: Restore dependencies
        run: dotnet restore ./PetSearchHome.sln

      - name: Build solution
        run: dotnet build ./PetSearchHome.sln --no-restore --configuration Release

      - name: Run tests
        run: dotnet test ./tests/PetSearchHome.BLL.Tests/PetSearchHome.BLL.Tests.csproj --no-build --verbosity normal
